/* Render config for Render Static Sites */
{
  "name": "kargobridge",
  "build": {
    "command": "bun install && bun run build",
    "publish": "dist"
  },
  "routes": [
    { "type": "rewrite", "source": "/*", "destination": "/index.html" }
  ]
}
